
## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. Set the `GEMINI_API_KEY` in [.env.local](.env.local) to your Gemini API key
3. Run the app:
   `npm run dev`

## Frontend vibe-coding instructions (React)

If you want to keep the backend template (`server/` + `Dockerfile`) and swap in a new React frontend, follow:

- [`FRONTEND_VIBE_AGENT_INSTRUCTIONS.md`](./FRONTEND_VIBE_AGENT_INSTRUCTIONS.md)
